# Aocam Dashboard Web

This component will become the browser-based security room UI.

Delivery 0 only defines the boundary and keeps a static placeholder page. The first real dashboard arrives after the single-phone camera node is reliable.
