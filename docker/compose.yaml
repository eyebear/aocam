services:
  local-server:
    build:
      context: ..
      dockerfile: docker/local-server.Dockerfile
    ports:
      - "8765:8765"
    command: ["python3", "/app/local-server/src/aocam_server/main.py", "--host", "0.0.0.0", "--port", "8765"]
