# Aocam Shared Contracts

Shared contracts keep the Android camera node, local server, and dashboard aligned.

Current stubs:

- `openapi/camera-node.v0.yaml`
- `openapi/server.v0.yaml`

These are intentionally small in Delivery 0. They will expand as each API is implemented.
